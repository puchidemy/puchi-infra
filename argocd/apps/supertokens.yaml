apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: supertokens
  namespace: argocd
spec:
  project: puchi
  source:
    repoURL: 'https://github.com/puchidemy/puchi-infra'
    targetRevision: main
    path: infra/supertokens
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: puchi-infra
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
