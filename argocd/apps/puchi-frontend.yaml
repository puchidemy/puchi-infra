apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: puchi-frontend
  namespace: argocd
spec:
  project: puchi
  source:
    repoURL: 'https://github.com/puchidemy/puchi-infra'
    targetRevision: main
    path: infra/frontend/web
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: puchi-frontend
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
