apiVersion: v1
kind: ConfigMap
metadata:
  name: notification-config
  namespace: puchi-backend
data:
  config.yaml: |
    server:
      http:
        addr: 0.0.0.0:8000
        timeout: 1s
      grpc:
        addr: 0.0.0.0:9000
        timeout: 1s
    data:
      database:
        driver: postgres
        source: postgres://puchi:puchi-db-prod@pg-puchi-rw.puchi-db.svc.cluster.local:5432/puchi?sslmode=disable
      redis:
        addr: valkey-node-0.platform.svc.cluster.local:6379
        read_timeout: 0.2s
        write_timeout: 0.2s
    auth:
      auth_service_url: "http://auth-service.puchi-backend:8000"
      public_paths:
        - /v1/health
        - /v1/healthz
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification
  namespace: puchi-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification
  template:
    metadata:
      labels:
        app: notification
    spec:
      containers:
        - name: notification
          image: ghcr.io/puchidemy/puchi-notification:git-c512816
          ports:
            - containerPort: 8000
            - containerPort: 9000
          command: ["./notification", "-conf", "/data/conf"]
          volumeMounts:
            - name: config
              mountPath: /data/conf
              readOnly: true
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
      volumes:
        - name: config
          configMap:
            name: notification-config
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: puchi-backend
spec:
  selector:
    app: notification
  ports:
    - name: grpc
      port: 8000
      targetPort: 8000
    - name: debug
      port: 9000
      targetPort: 9000
