apiVersion: v1
kind: ConfigMap
metadata:
  name: grading-config
  namespace: puchi-backend
data:
  config.yaml: |
    server:
      http:
        addr: 0.0.0.0:8000
        timeout: 1s
      grpc:
        addr: 0.0.0.0:9000
        timeout: 1s
    data:
      database:
        driver: postgres
        source: postgres://puchi:puchi-db-prod@pg-puchi-rw.puchi-db.svc.cluster.local:5432/puchi?sslmode=disable
      redis:
        addr: valkey-node-0.platform.svc.cluster.local:6379
        read_timeout: 0.2s
        write_timeout: 0.2s
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: grading
  namespace: puchi-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grading
  template:
    metadata:
      labels:
        app: grading
    spec:
      containers:
        - name: grading
          image: ghcr.io/puchidemy/puchi-grading:git-544928b
          ports:
            - containerPort: 8000
            - containerPort: 9000
          command: ["./grading", "-conf", "/data/conf"]
          volumeMounts:
            - name: config
              mountPath: /data/conf
              readOnly: true
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
      volumes:
        - name: config
          configMap:
            name: grading-config
---
apiVersion: v1
kind: Service
metadata:
  name: grading-service
  namespace: puchi-backend
spec:
  selector:
    app: grading
  ports:
    - name: grpc
      port: 8000
      targetPort: 8000
    - name: debug
      port: 9000
      targetPort: 9000
